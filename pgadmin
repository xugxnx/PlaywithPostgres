FROM dpage/pgadmin4
USER root
RUN mkdir -p /var/lib/pgadmin/storage/user_example.com
COPY --chown=pgadmin:pgadmin ./pgpass /var/lib/pgadmin/storage/user_example.com/
COPY --chown=pgadmin:pgadmin ./servers.json /pgadmin4/
USER pgadmin